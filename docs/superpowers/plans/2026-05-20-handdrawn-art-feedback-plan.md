# Handdrawn Art Feedback Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver the first playable pass of the approved handdrawn polish direction without changing game rules.

**Architecture:** Keep the existing React structure and add targeted visual hooks in `src/App.tsx`, with most polish implemented in `src/App.css`. Use the existing static style tests to lock in the new handdrawn design system, feedback classes, and reduced-motion guardrails.

**Tech Stack:** React 19, Vite, TypeScript, Vitest, CSS, existing canvas battle effects.

---

### Task 1: Lock First-Pass Visual Requirements With Tests

**Files:**
- Modify: `src/App.css.test.ts`
- Modify: `src/App.structure.test.ts`

- [ ] Add CSS assertions for handdrawn tokens: paper texture variables, sketch shadows, paper rotation values, sticker/tape selectors, `prefers-reduced-motion`, and non-rectangular paper-card hooks.
- [ ] Add structure assertions for first-pass feedback hooks: mode cards, dog cards, shop cards, inventory board, battle stage, battle dogs, battle dice, and result panel must expose handdrawn classes.
- [ ] Run `npm test -- src/App.css.test.ts src/App.structure.test.ts` and confirm the new tests fail because the hooks/styles do not exist yet.

### Task 2: Add Handdrawn Visual Hooks

**Files:**
- Modify: `src/App.tsx`

- [ ] Add stable class hooks to existing components without changing behavior: `paper-card`, `sketch-panel`, `sticker-card`, `handdrawn-stage`, `handdrawn-dice`, `handdrawn-result`, `paper-shop-card`, `paper-dog-card`, and `paper-inventory`.
- [ ] Keep all existing click, drag, tooltip, battle playback, and API behavior unchanged.
- [ ] Run the targeted tests and confirm any remaining failures are style-only.

### Task 3: Implement First-Pass Handdrawn CSS

**Files:**
- Modify: `src/App.css`

- [ ] Add root-level handdrawn tokens for paper texture, sketch shadows, soft table background, sticker colors, and feedback colors.
- [ ] Add paper texture and table surface treatment to `body`, `.app-shell`, and major panels.
- [ ] Add soft irregular paper-card treatment for mode cards, dog cards, shop cards, choice cards, reward cards, floating tips, and result panels using pseudo-elements and small rotations.
- [ ] Add high-frequency feedback: pressed button bounce, hover lift, drag paper tilt, slot placement glow, sell-zone sketch emphasis, upgrade star sticker, and shop price tag treatment.
- [ ] Add battle polish: tabletop stage, soft spotlight, dog status overlays, handdrawn damage number styling in canvas-compatible colors, stronger active equipment glow, dice landing feel, and result-card flip.
- [ ] Add `@media (prefers-reduced-motion: reduce)` rules that suppress non-essential animations and transitions.
- [ ] Run targeted tests and fix CSS omissions.

### Task 4: Verify and Build

**Files:**
- Generated by build: `dist-click/DogFight-standalone.cmd`

- [ ] Run `npm test -- src/App.css.test.ts src/App.structure.test.ts`.
- [ ] Run `npm test`.
- [ ] Run `npm run build` because the change affects frontend display and the standalone playable file.
- [ ] Confirm `E:\AI-GPT\DogFight\dist-click\DogFight-standalone.cmd` is regenerated.

### Task 5: Final Review

**Files:**
- Review: `src/App.tsx`
- Review: `src/App.css`
- Review: `src/App.css.test.ts`
- Review: `src/App.structure.test.ts`

- [ ] Check that the style remains the existing soft handdrawn DogFight direction, not a new dark, neon, comic, or arcade direction.
- [ ] Check that high-frequency layout areas remain stable for drag/drop and mobile layout.
- [ ] Summarize changed files, tests, build result, and standalone output path.
