# Shop Tab Depth Variants Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add two more visual variants for shop choice tabs and shop paper cards, while strengthening contact shadows and quality glow depth.

**Architecture:** This is a focused CSS change. `src/App.css.test.ts` locks the visual structure with string-based CSS rule assertions, and `src/App.css` implements the added variants without changing JSX or game data.

**Tech Stack:** React 19, Vite, TypeScript, Vitest, CSS `nth-child`, `clip-path`, `filter: drop-shadow()`, `box-shadow`, `perspective`.

---

## File Structure

- Modify: `src/App.css.test.ts`
  - Adds failing CSS structure tests for two extra shop choice styles and two extra shop card styles.
- Modify: `src/App.css`
  - Adds light perspective and depth variables to shop choice cards.
  - Adds two extra `nth-child` visual variants to shop choice cards and shop paper cards.
  - Strengthens paper contact shadow and quality glow layering.
- Generated by build: `dist-click/index.html`
- Generated by build: `dist-click/DogFight-standalone.cmd`
- Not modified: `C:\Users\User\Desktop\狗骰乱斗\狗骰乱斗.xlsx`
  - No equipment values, prices, combat parameters, economy, or balance model changes.

## Task 1: Add Failing CSS Structure Tests

**Files:**
- Modify: `src/App.css.test.ts`
- Test: `src/App.css.test.ts`

- [ ] **Step 1: Add tests for shop choice tab variants and shop paper depth**

Insert these tests near the existing shop CSS tests:

```ts
  it('adds two extra randomized paper styles to shop choice tabs', () => {
    expect(cssRule('.shop-choice-screen .choice-grid')).toContain('perspective')
    expect(cssRule('.shop-choice-screen .choice')).toContain('--choice-tab-contact-shadow')
    expect(cssRule('.shop-choice-screen .choice')).toContain('rotateX(var(--choice-tab-lift))')
    expect(cssRule('.shop-choice-screen .choice:nth-child(4n)')).toContain('--choice-tab-cut')
    expect(cssRule('.shop-choice-screen .choice:nth-child(4n)')).toContain('--choice-tab-contact-shadow')
    expect(cssRule('.shop-choice-screen .choice:nth-child(5n)')).toContain('--choice-tab-cut')
    expect(cssRule('.shop-choice-screen .choice:nth-child(5n)')).toContain('--choice-tab-paper')
    expect(cssRule('.shop-choice-screen .choice.placeholder')).toContain('transform: none')
  })

  it('adds two more shop paper card silhouettes while preserving quality glow depth', () => {
    expect(cssRule('.paper-shop-card')).toContain('--shop-paper-depth-filter-hover')
    expect(cssRule('.paper-shop-card')).toContain('--shop-paper-contact-shadow')
    expect(cssRule('.paper-shop-card')).toContain('--shop-quality-glow-ring')
    expect(cssRule('.offer-row .paper-shop-card:nth-child(6n + 2)')).toContain('--shop-paper-cut')
    expect(cssRule('.offer-row .paper-shop-card:nth-child(6n + 2)')).toContain('--shop-paper-contact-shadow')
    expect(cssRule('.offer-row .paper-shop-card:nth-child(6n + 5)')).toContain('--shop-paper-cut')
    expect(cssRule('.offer-row .paper-shop-card:nth-child(6n + 5)')).toContain('--shop-paper-tone')
    expect(cssRule('.paper-shop-card:hover::before')).toContain('var(--shop-paper-shadow-hover)')
  })
```

- [ ] **Step 2: Run the focused CSS test and verify RED**

Run:

```bash
npx vitest run src/App.css.test.ts
```

Expected: `FAIL`. The new assertions should fail because `.shop-choice-screen .choice-grid`, `.shop-choice-screen .choice:nth-child(4n)`, `.shop-choice-screen .choice:nth-child(5n)`, `.offer-row .paper-shop-card:nth-child(6n + 2)`, and `.offer-row .paper-shop-card:nth-child(6n + 5)` do not yet contain the requested depth and variant variables.

## Task 2: Implement Shop Choice Tab Variants

**Files:**
- Modify: `src/App.css`
- Test: `src/App.css.test.ts`

- [ ] **Step 1: Add light perspective to the shop choice grid**

Update the `.choice-grid` rule used under `.shop-choice-screen` by adding a more specific rule:

```css
.shop-choice-screen .choice-grid {
  perspective: 900px;
  transform-style: preserve-3d;
}
```

- [ ] **Step 2: Add tab depth variables to shop choice cards**

Add this block after the existing `.choice` rules:

```css
.shop-choice-screen .choice {
  --choice-tab-lift: 4deg;
  --choice-tab-paper:
    var(--paper-grain),
    linear-gradient(180deg, rgba(255, 253, 245, .96), rgba(241, 220, 184, .94));
  --choice-tab-cut: polygon(1% 2%, 36% 0, 99% 2%, 100% 78%, 96% 100%, 28% 98%, 0 96%);
  --choice-tab-contact-shadow:
    0 2px 0 rgba(255, 255, 255, .5),
    0 9px 0 -3px rgba(61, 45, 37, .22),
    0 20px 28px -14px rgba(61, 45, 37, .28);
  transform: rotateX(var(--choice-tab-lift)) rotate(var(--paper-tilt, 0deg));
  transform-origin: 50% 82%;
  background: var(--choice-tab-paper);
  box-shadow: var(--choice-tab-contact-shadow), inset 0 -7px 0 rgba(112, 72, 30, .13), inset 0 2px 0 rgba(255, 255, 255, .56);
  clip-path: var(--choice-tab-cut);
}

.shop-choice-screen .choice:hover {
  transform: translateY(-2px) rotateX(var(--choice-tab-lift)) rotate(var(--paper-tilt, 0deg));
  box-shadow: var(--choice-tab-contact-shadow), 0 20px 30px rgba(112, 72, 30, .18), inset 0 -7px 0 rgba(112, 72, 30, .13);
}
```

- [ ] **Step 3: Add the two new random-looking tab variants**

Add:

```css
.shop-choice-screen .choice:nth-child(4n) {
  --paper-tilt: .72deg;
  --choice-tab-lift: 6deg;
  --choice-tab-cut: polygon(0 4%, 24% 0, 76% 2%, 100% 0, 98% 88%, 84% 100%, 2% 97%);
  --choice-tab-paper:
    var(--paper-grain),
    radial-gradient(circle at 18% 72%, rgba(255, 219, 170, .34), transparent 32%),
    linear-gradient(179deg, #fff9ec, #ecd2a8);
  --choice-tab-contact-shadow:
    2px 3px 0 rgba(255, 255, 255, .42),
    4px 11px 0 -4px rgba(61, 45, 37, .24),
    0 24px 30px -16px rgba(61, 45, 37, .3);
}

.shop-choice-screen .choice:nth-child(5n) {
  --paper-tilt: -.86deg;
  --choice-tab-lift: 5deg;
  --choice-tab-cut: polygon(2% 0, 48% 2%, 69% 0, 99% 3%, 100% 72%, 97% 99%, 54% 100%, 18% 98%, 0 94%);
  --choice-tab-paper:
    var(--paper-grain),
    radial-gradient(circle at 76% 18%, rgba(255, 245, 190, .5), transparent 28%),
    linear-gradient(181deg, #fffdf6, #efd8bb);
}

.shop-choice-screen .choice.placeholder {
  transform: none;
  clip-path: none;
}
```

- [ ] **Step 4: Run focused CSS test and verify this task**

Run:

```bash
npx vitest run src/App.css.test.ts
```

Expected: The shop choice tab test passes. The shop paper card test may still fail until Task 3 is complete.

## Task 3: Implement Extra Shop Paper Card Variants and Depth

**Files:**
- Modify: `src/App.css`
- Test: `src/App.css.test.ts`

- [ ] **Step 1: Strengthen base contact shadow**

In `.paper-shop-card`, update `--shop-paper-contact-shadow` and `--shop-paper-contact-shadow-hover` so they retain a visible grounded shadow:

```css
  --shop-paper-contact-shadow:
    0 2px 0 rgba(255, 255, 255, .46),
    3px 8px 0 -2px rgba(61, 45, 37, .22),
    0 18px 26px -10px rgba(61, 45, 37, .28);
  --shop-paper-contact-shadow-hover:
    0 2px 0 rgba(255, 255, 255, .52),
    4px 11px 0 -3px rgba(61, 45, 37, .22),
    0 25px 34px -12px rgba(61, 45, 37, .3);
```

- [ ] **Step 2: Add the two extra paper card variants**

Add after the existing `.offer-row .paper-shop-card:nth-child(5)` rule:

```css
.offer-row .paper-shop-card:nth-child(6n + 2) {
  --paper-tilt: .82deg;
  --shop-paper-y: -3px;
  --shop-paper-radius: 14px 8px 12px 11px;
  --shop-paper-cut: polygon(0 4%, 26% 0, 52% 2%, 78% 0, 100% 4%, 98% 88%, 84% 100%, 38% 98%, 2% 96%);
  --shop-paper-contact-shadow:
    2px 3px 0 rgba(255, 255, 255, .4),
    5px 11px 0 -4px rgba(61, 45, 37, .24),
    0 24px 32px -15px rgba(61, 45, 37, .3);
}

.offer-row .paper-shop-card:nth-child(6n + 5) {
  --paper-tilt: -1.08deg;
  --shop-paper-y: 2px;
  --shop-paper-radius: 8px 15px 10px 14px;
  --shop-paper-cut: polygon(2% 0, 38% 2%, 65% 0, 99% 3%, 100% 74%, 97% 99%, 55% 100%, 18% 98%, 0 94%);
  --shop-paper-tone:
    var(--paper-grain),
    radial-gradient(circle at 80% 20%, rgba(255, 244, 190, .42), transparent 30%),
    linear-gradient(182deg, #fffdf5, #eecfa7);
}
```

- [ ] **Step 3: Run focused CSS test and verify GREEN**

Run:

```bash
npx vitest run src/App.css.test.ts
```

Expected: `PASS`.

## Task 4: Build and Package

**Files:**
- Generated: `dist-click/index.html`
- Generated: `dist-click/DogFight-standalone.cmd`

- [ ] **Step 1: Run full build**

Run:

```bash
npm run build
```

Expected: `guard:chains`, `tsc -b`, `vite build`, and `tsx scripts/package-click-index.mjs` complete with exit code 0.

- [ ] **Step 2: Confirm no Excel update is needed**

Do not edit:

```text
C:\Users\User\Desktop\狗骰乱斗\狗骰乱斗.xlsx
```

Reason: this change only affects CSS/front-end presentation and generated playable output.
